module.exports = app => {
    app.beforeStart(async () => {
        // app.allDetails = await app.curl('127.0.0.1:8080/api/all', {
        //     method: 'get',
        //     dataType: 'json'
        // })

        // console.log(app.allDetails)
    })
}